packages:
  - "."

# Allow packages that need native postinstall build scripts (required on Vercel CI)
onlyBuiltDependencies:
  - esbuild
  - sharp
  - workerd

allowBuilds:
  esbuild: true
  sharp: true
  workerd: true
